const permissions = require("./permissions");
const types = require("./types");
const presence = require("./presence");
const platforms = require("./platforms");

module.exports = { permissions, types, presence, platforms };
